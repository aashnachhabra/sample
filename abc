import logging
import traceback
import time
import re
import json
from typing import Dict, Any, Tuple

import allure
from playwright.sync_api import expect
from tests.vikings.conftest import THRESHOLD
from tests.vikings.fw.common.Results import Results
from tests.vikings.fw.ui.BasePage import BasePage
from tests.vikings.fw.utils import get_multiple_scores


class BeccaPage(BasePage):
    CHAT_INPUT_SELECTOR = ".input-section textarea[matInput]"
    SUBMIT_BUTTON_SELECTOR = "button.submit-button"
    START_NEW_CHAT_SELECTOR = "button:has-text('Start New Chat')"
    CHAT_MESSAGE_SELECTOR = "div.chat-box app-chat-message"

    GENERATION_TIMEOUT_SECS = 180
    INPUT_READY_TIMEOUT_MS = 60000
    ACTION_TIMEOUT_MS = 10000

    def __init__(self, page):
        super().__init__(page)
        self._console_logs = []
        try:
            self.page.on("console", lambda m: self._console_logs.append(f"[{m.type}] {m.text}"))
        except Exception:
            pass

    def open_becca(self):
        with allure.step("Setup"):
            self.page.wait_for_load_state("domcontentloaded")
            self._wait_for_chat_input_ready(timeout=self.INPUT_READY_TIMEOUT_MS)
            allure.attach(self.page.url, "URL", allure.attachment_type.TEXT)
            self.attach_screenshot("Setup Screenshot")
            self.page.wait_for_timeout(1500)


    def start_new_chat_if_present(self):
        with allure.step("Start New Chat"):
            btn = self.page.locator(self.START_NEW_CHAT_SELECTOR)
            try:
                if btn.count() == 0:
                    return
                btn.first.wait_for(state="visible", timeout=3000)
                btn.first.click()
                self.page.wait_for_function(
                    "document.querySelectorAll('app-chat-message').length === 0",
                    timeout=5000
                )
                self.page.wait_for_timeout(1000)
                self.attach_screenshot("After Start New Chat")
            except Exception:
                pass

    def _wait_for_chat_input_ready(self, timeout: int = 60000):
        """
        Wait until the chat textarea and submit button are both usable.
        This is more reliable than only checking `visible`.
        """
        input_box = self.page.locator(self.CHAT_INPUT_SELECTOR).first
        submit_btn = self.page.locator(self.SUBMIT_BUTTON_SELECTOR).first

        try:
            input_box.wait_for(state="attached", timeout=timeout)
            expect(input_box).to_be_visible(timeout=timeout)
            expect(input_box).to_be_editable(timeout=timeout)

            submit_btn.wait_for(state="attached", timeout=timeout)
            expect(submit_btn).to_be_visible(timeout=timeout)
            expect(submit_btn).to_be_enabled(timeout=timeout)

            return input_box, submit_btn

        except Exception:
            allure.attach(self.page.url, "Current URL", allure.attachment_type.TEXT)
            self.attach_screenshot("Chat input not ready")
            raise

    def _wait_for_generation_complete(self):
        """
        Wait until the submit button text becomes 'Submit' again.
        This is used as the completion signal after generation ends.
        """
        self.page.wait_for_function(
            """() => {
                const btn = document.querySelector('button.submit-button');
                return btn && btn.innerText && btn.innerText.trim() === 'Submit';
            }""",
            timeout=self.GENERATION_TIMEOUT_SECS * 1000
        )

    def _get_stable_latest_message(self, timeout_secs: int = 5) -> str:
        """
        Read the latest chat message and wait briefly until the text stops changing.
        """
        last = self.page.locator(self.CHAT_MESSAGE_SELECTOR).last
        last.wait_for(state="visible", timeout=30000)

        previous = ""
        end = time.time() + timeout_secs

        while time.time() < end:
            current = last.inner_text().strip()
            if current == previous:
                return current
            previous = current
            time.sleep(0.5)

        return previous

    def _strip_ui_chrome(self, text: str) -> str:
        noise = [
            "content_copy",
            "star_border",
            "thumb_up_off_alt",
            "thumb_down_off_alt",
        ]
        for n in noise:
            text = text.replace(n, "")
        return text.strip()

    def _format_prompt_with_testcase(self, prompt: str, testcase: Dict[str, Any]) -> str:
        """
        Replaces $ColumnName placeholders using testcase values.
        Example: $PlanCode -> DY82
        """
        if not prompt:
            return ""

        def replacer(match):
            key = match.group(1)
            for col, val in testcase.items():
                if col.lower() == key.lower() and str(val).strip():
                    return str(val).strip()
            return match.group(0)

        return re.sub(r"\$(\w+)", replacer, prompt).strip()

    def _normalize(self, text: str) -> str:
        if not text:
            return ""
        text = text.lower()
        text = re.sub(r"[^\w\s%$.-]", " ", text)
        text = re.sub(r"\s+", " ", text)
        return text.strip()

    def _send_prompt_once(self, prompt: str, start_new_chat: bool = True) -> Tuple[str, float]:
        if start_new_chat:
            self.start_new_chat_if_present()
        input_box, submit_btn = self._wait_for_chat_input_ready(timeout=self.INPUT_READY_TIMEOUT_MS)
        with allure.step("Type Prompt"):
            allure.attach(prompt, "Prompt", allure.attachment_type.TEXT)
            try:
                input_box.click(timeout=self.ACTION_TIMEOUT_MS)
                input_box.fill("", timeout=self.INPUT_READY_TIMEOUT_MS)
                input_box.fill(prompt, timeout=self.INPUT_READY_TIMEOUT_MS)
            except Exception:
                self.page.wait_for_timeout(2000)
                input_box, submit_btn = self._wait_for_chat_input_ready(timeout=self.INPUT_READY_TIMEOUT_MS)
                input_box.click(timeout=self.ACTION_TIMEOUT_MS)
                input_box.fill("", timeout=self.INPUT_READY_TIMEOUT_MS)
                input_box.fill(prompt, timeout=self.INPUT_READY_TIMEOUT_MS)
            self.attach_screenshot("Prompt Typed")
        with allure.step("Submit And Wait"):
            start_time = time.time()
            submit_btn.click(timeout=self.ACTION_TIMEOUT_MS)
            self._wait_for_generation_complete()
            response = self._get_stable_latest_message()
            clean_response = self._strip_ui_chrome(response)
            elapsed_secs = round(time.time() - start_time, 2)
            allure.attach(str(elapsed_secs), "Response Time (secs)", allure.attachment_type.TEXT)
            allure.attach(clean_response, "Raw Response", allure.attachment_type.TEXT)
            self.attach_screenshot("Becca Response")
        return clean_response, elapsed_secs

    def send_prompt_and_wait(self, prompt: str) -> Tuple[str, float]:
        """
        Sends the prompt.
        If the first response is the known transient bootstrap message,
        retry once in the same chat window without starting a new chat again.
        """
        actual_response, elapsed_secs = self._send_prompt_once(prompt, start_new_chat=True)

        if "Unable to connect to the server" in actual_response:
            logging.warning(
                "First prompt hit connection/bootstrap issue. Retrying once in same chat window..."
            )
            self.page.wait_for_timeout(2000)
            actual_response, elapsed_secs = self._send_prompt_once(prompt, start_new_chat=False)

        return actual_response, elapsed_secs

    def run_prompt_and_get_response(self, prompt: str) -> Tuple[str, float]:
        return self.send_prompt_and_wait(prompt)

    def _validate_response(
            self,
            actual_response: str,
            expected_answer: str,
            validation_text: str,
            testcase: Dict[str, Any],
    ) -> Tuple[str, str]:
        if not actual_response or self._normalize(actual_response) in {"", "none"}:
            allure.attach("ACTUAL_RESPONSE is empty", "UI Validation Details", allure.attachment_type.TEXT)
            return "failed", "ACTUAL_RESPONSE is empty"
        actual_norm = self._normalize(actual_response)
        if not validation_text or not validation_text.strip():
            self._attach_json("UI Validation Details", {
                "mode": "none",
                "actual_norm": actual_norm,
                "note": "No validation_text provided"
            })
            return "passed", ""
        tokens_raw = [t.strip() for t in validation_text.split(",") if t.strip()]
        mode_any = False
        if tokens_raw and tokens_raw[0].lower().startswith("any:"):
            mode_any = True
            tokens_raw[0] = tokens_raw[0][4:].strip()
        col_map = {
            k.lower(): str(v).strip()
            for k, v in testcase.items()
            if str(v).strip()
        }
        resolved_tokens = [col_map.get(tok.lower(), tok) for tok in tokens_raw]
        normalized_tokens = [self._normalize(tok) for tok in resolved_tokens if tok]
        if mode_any:
            hit = any(tok and tok in actual_norm for tok in normalized_tokens)
            self._attach_json("UI Validation Details", {
                "mode": "any",
                "tokens": resolved_tokens,
                "normalized_tokens": normalized_tokens,
                "actual_norm": actual_norm,
                "hit": hit
            })
            if not hit:
                return "failed", f"Missing ANY of keywords from validation text in ACTUAL_RESPONSE: {resolved_tokens}"
            return "passed", ""
        else:
            missing = [
                orig for tok, orig in zip(normalized_tokens, resolved_tokens)
                if tok and tok not in actual_norm
            ]
            self._attach_json("UI Validation Details", {
                "mode": "all",
                "tokens": resolved_tokens,
                "normalized_tokens": normalized_tokens,
                "actual_norm": actual_norm,
                "missing": missing
            })
            if missing:
                return "failed", f"Missing keywords from validation text in ACTUAL_RESPONSE: {missing}"
            return "passed", ""

    def execute_testcase(self, testcase: Dict[str, Any], rows_accumulator: Dict[str, Any]):
        raw_prompt = str(testcase.get("prompt", "")).strip()
        expected_answer = str(testcase.get("expected_answer", "")).strip()
        validation_text = str(testcase.get("validation_text", "")).strip()
        formatted_prompt = self._format_prompt_with_testcase(raw_prompt, testcase)
        row_key = testcase.get("id") or formatted_prompt
        title_seed = testcase.get("title") or testcase.get("name") or formatted_prompt[:140]
        try:
            allure.dynamic.title(title_seed)
        except Exception:
            pass
        try:
            allure.dynamic.feature("BECCA v2")
            if testcase.get("id"):
                allure.dynamic.story(str(testcase["id"]))
            allure.dynamic.label("suite", "test_becca_eval_pipeline")
            allure.dynamic.label("application", "becca_v2")
            sev = str(testcase.get("severity", "critical")).lower()
            allure.dynamic.severity(sev if sev in {"blocker","critical","normal","minor","trivial"} else "critical")
        except Exception:
            pass
        actual_response = ""
        time_taken = 0
        ui_status = "failed"
        ui_error = ""
        with allure.step("Ask Question in Becca"):
            try:
                actual_response, time_taken = self.run_prompt_and_get_response(formatted_prompt)
                ui_status, ui_error = self._validate_response(
                    actual_response=actual_response,
                    expected_answer=expected_answer,
                    validation_text=validation_text,
                    testcase=testcase,
                )
            except Exception as e:
                logging.error(e)
                allure.attach(traceback.format_exc(), "UI Exception", allure.attachment_type.TEXT)
                if not actual_response:
                    actual_response = "error while fetching ui response"
                ui_status = "failed"
                ui_error = str(e)
        ui_row = {
            **testcase,
            "actual_prompt": formatted_prompt,
            "expected_response": expected_answer,
            "actual_response": actual_response,
            "ui_status": ui_status,
            "ui_error": ui_error,
            "Response time(secs)": time_taken,
            "validation_text": validation_text,
        }
        rows_accumulator[row_key] = ui_row
        with allure.step("UI Artifacts"):
            self._attach_json("UI Row", ui_row)
            if self._console_logs:
                allure.attach("\n".join(self._console_logs[-50:]), "Console (last 50)", allure.attachment_type.TEXT)
        return ui_row

    def evaluate_testcase(self, ui_row: Dict[str, Any], rows_accumulator: Dict[str, Any]):
        with allure.step("Evaluate Becca Testcase"):

            if ui_row["ui_status"] != "passed":
                ui_row.update({
                    "eval_status": "skipped",
                    "eval_error": "UI validation failed",
                    "eval_threshold": "",
                    "gEval_score": "",
                    "gEval_reason": "",
                })

                allure.attach(
                    "Informational eval: skipped (UI validation failed)",
                    name="INFO",
                    attachment_type=allure.attachment_type.TEXT
                )
                return ui_row

            threshold = THRESHOLD
            expected = ui_row.get("expected_response", "").strip()

            payload = {
                "question": ui_row["actual_prompt"],
                "llmAnswer": ui_row["actual_response"],
                "groundAnswer": expected,
                "contextualPrecision": False,
                "contextualRecall": False,
                "faithfulness": False,
                "gEval": True,
            }

            data = get_multiple_scores(payload)
            score = data.get("gEval", {}).get("score", 0)
            reason = data.get("gEval", {}).get("reason", "")

            eval_status = "passed" if score >= threshold else "failed"

            ui_row.update({
                "eval_threshold": threshold,
                "gEval_score": score,
                "gEval_reason": reason,
                "eval_status": eval_status,
                "eval_error": "",
            })
            if eval_status != "passed":
                raise AssertionError(
                    f"Eval failed | score={score}, threshold={threshold}\nReason: {reason}"
                )
            Results.report_event(f"Eval Threshold: {threshold}")
            Results.report_event(f"Eval Result: gEval score={score} | reason={reason}")
            Results.report_event(f"Informational eval: {eval_status}")
        return ui_row

    def _attach_json(self, name: str, obj: Dict[str, Any]):
        try:
            allure.attach(json.dumps(obj, indent=2, ensure_ascii=False), name, allure.attachment_type.JSON)
        except Exception:
            allure.attach(str(obj), name, allure.attachment_type.TEXT)


current becca page what to change and exactly where for this
