function validatePlans() {
let checkedArray = datatable.getRows();
for (var i = 0; i < checkedArray.length; i++) {
        if (checkedArray[i][0].value === 'true') {
        if(checkedArray[i][6].content !== 'Withdrawn')
        $("#validatePlansCfrmBoxModal").modal("toggle");
        else
        $("#cannotValidateModal").modal("toggle")
    }
}
}

i can select multiple plans but if any of the plans have status withdrawn i want to show cannotValidateModal modal and if none are withdrawn status then show validatePlansCfrmBoxModal. Please fix the above function 
