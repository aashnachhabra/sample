setDefaultDropdownOption(option: any) {
    console.log(option)
    this.optionsArray.forEach((item: any) => {
      item.isDefault = false
    })
    console.log(this.optionsArray)
    option.isDefault = true;
  }
