
<div class="form-group">
                <label for="planStatus" class="required">Plan Status</label>
                <select class="form-select" ng-model="requestBody.planDetails.planStatus" id="planStatus"
                        name="planStatus" ng-disabled="requestBody.parentPlanAssignmentID != null">
                    <option value="">Select...</option>
                    <option
                            ng-repeat="(key, value) in portfolioEditorDDMapResponse.planStatusMap"
                            ng-selected="requestBody.planDetails.planStatus == value" value="{{value}}">{{value}}
                    </option>
                </select>
            </div>

<button type="button" class="btn btn-outline-primary" ng-cloak ng-show="requestBody.planAssignmentID != null" ng-click="sendToValidate()">Validate
		                    </button>

if planstatus is changed to Withdrawn i want to disable this validate button and its not Withdrawn then enable it
