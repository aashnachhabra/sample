 if (limitMgmtRequest == null) throw new Exception("limitMgmtRequest should not be Empty or null..!");

        HttpSession session = request.getSession();
        session.setAttribute("limitMgmtRequest", limitMgmtRequest);
return ResponseEntity.status(HttpStatus.OK).body(Collections.singletonMap("limitMsg", "SUCCESS"));
    }
