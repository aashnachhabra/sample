<div class="row">
    <div class="col">
       <div class="mb-3">
          <label class="form-label">Region</label>
          <div class="form-floating">
             <select class="form-select" id="region">
                <c:forEach var="region"
                   items="${planDD.regionMap}">
                   <option id="${region.key}" name="${region.value}"
                      value="${region.value}">${region.value}
                      </option>
                </c:forEach>
             </select>
          </div>
       </div>
    </div>

</div>
