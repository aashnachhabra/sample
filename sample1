 <div class="form-group mb-3">
      <ng-select [items]="filteredItems" bindLabel="displayName" [searchable]="true" (input)="onSearchChange($event)"
        (change)="onItemSelected($event)">
        <ng-template ng-label-tmp let-item="item">
          {{ item.displayName }}
        </ng-template>
        <ng-template ng-option-tmp let-item="item" let-index="index">
          {{ item.displayName }}
        </ng-template>
      </ng-select>
    </div>
